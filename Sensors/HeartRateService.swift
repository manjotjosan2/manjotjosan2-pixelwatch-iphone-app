import Foundation

class HeartRateService {
    func processHeartRateData(_ value: Int) {
        // Handle heart rate sample (update dashboard, send to paired watch, etc)
    }
    func syncHeartRate(_ value: Int) {
    // Send heart rate to watch over BLE using protocol spec
}
}
