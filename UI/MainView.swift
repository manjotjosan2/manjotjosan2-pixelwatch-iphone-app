import SwiftUI

struct MainView: View {
    var body: some View {
        VStack {
            Text("Pixel Watch iPhone App")
                .font(.title)
                .padding()
            // Add more UI components here!
        }
    }
}

struct MainView_Previews: PreviewProvider {
    static var previews: some View {
        MainView()
    }
}
